<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stylefeng.guns.modular.system.dao.UserInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.stylefeng.guns.modular.system.model.UserInfo">
        <id column="id" property="id" />
        <result column="email" property="email" />
        <result column="user_name" property="userName" />
        <result column="department" property="department" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, email, user_name, department
    </sql>

    <select id="searchByUserName" resultType="com.stylefeng.guns.modular.system.model.UserInfo">
        select * from `user_info` as  userName where `user_name` like #{userName};
    </select>

</mapper>
